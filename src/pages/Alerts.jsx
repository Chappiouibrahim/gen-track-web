import React from "react";
import { Typography } from "@mui/material";

export default function Alerts() {
  return <Typography variant="h4">Page Alertes — on implémente le CRUD ensuite</Typography>;
}
